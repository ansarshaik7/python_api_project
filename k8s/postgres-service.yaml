apiVersion: v1
kind: Service
metadata:
  name: pythonapipostgres-service
spec:
  selector:
    app: pythonapipostgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP