apiVersion: v1
kind: Service
metadata:
  name: pythonapipostgres-service
  namespace: pythonapiapp
spec:
  selector:
    apps: pythonapipostgres
  ports:
    - protocol: TCP
      port: 5432
      targetPort: 5432
  type: ClusterIP